{ Test: Uso de ELSE }
PROGRAM TestElse
VAR
    x : INT;
    y : INT;
    soyString : STRING;

BEGIN
    x := 5;
    y := 5;

    x := 1+2+3+4+5;

    IF x = 15 THEN
        BEGIN   
            WRITE(soyString);
        END
    ELSE
        BEGIN
            WRITE("No existe");
        END
END.
